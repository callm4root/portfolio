AOS.init({
  duration: 100,
  once: false,    // false = animation rejouée à chaque fois que l'élément entre dans le viewport
  mirror: true    // permet aussi de rejouer l'animation quand on remonte (scroll up)
});


